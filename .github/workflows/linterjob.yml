name: Pylinter job

on:
  
 push:
   branches: [ "main ]

 workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Analysing the code with pylint
      run: |
        cd cloud-flask-task2
        pip install -r requirements.txt
        pylint flasktask2.py
